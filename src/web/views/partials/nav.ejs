<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-minimal">
  <div class="container-fluid navbar-minimal__shell">
    <%
      const safeAdminUser = typeof adminUser !== 'undefined' ? adminUser : null;
      const safeDiscordUser = typeof discordUser !== 'undefined' ? discordUser : null;
      const safeCsrfToken = typeof csrfToken !== 'undefined' ? csrfToken : '';
    %>
    <a class="navbar-brand" href="<%= safeAdminUser ? '/admin/dashboard' : '/' %>">
      <span class="brand">
        <span class="brand-icon brand-icon-sm" data-fallback="RS">
          <img src="/assets/images/bots/Rodstarkian_Bot.png" alt="Rodstarkian Bot logo" loading="lazy" data-fallback-img />
          <span class="brand-fallback">RS</span>
        </span>
        <span class="brand-title">Rodstarkian Suite</span>
      </span>
    </a>
    <%
      const safeActiveGuildId = typeof activeGuildId !== 'undefined' ? activeGuildId : '';
      const safeActiveGuildName = typeof activeGuildName !== 'undefined' ? activeGuildName : '';
      const hasActiveGuild = safeAdminUser && safeActiveGuildId;
    %>
    <% if (hasActiveGuild) { %>
      <span class="navbar-text small text-white-50 d-none d-lg-inline">
        Server: <%= safeActiveGuildName || safeActiveGuildId %>
      </span>
    <% } %>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse navbar-minimal__collapse" id="navbarNav">
      <% if (hasActiveGuild) { %>
        <div class="navbar-server-pill d-lg-none">
          <span class="navbar-server-pill__label">Server</span>
          <span class="navbar-server-pill__value"><%= safeActiveGuildName || safeActiveGuildId %></span>
        </div>
      <% } %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 navbar-minimal__primary">
        <% if (safeAdminUser) { %>
          <li class="nav-item"><a class="nav-link" href="/admin/servers">Servers</a></li>
          <% if (hasActiveGuild) { %>
            <li class="nav-item"><a class="nav-link" href="/admin/dashboard">Dashboard</a></li>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                God's Eye
              </a>
              <ul class="dropdown-menu dropdown-menu-dark">
                <li><a class="dropdown-item" href="/admin/verification/settings">Verification</a></li>
                <li><a class="dropdown-item" href="/admin/verification/iplogs">IP Logs</a></li>
              </ul>
            </li>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                RoBot
              </a>
              <ul class="dropdown-menu dropdown-menu-dark">
                <li><a class="dropdown-item" href="/admin/economy/users">Users</a></li>
              </ul>
            </li>

            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Rodstarkian Vault
              </a>
              <ul class="dropdown-menu dropdown-menu-dark">
                <li><a class="dropdown-item" href="/admin/backups">Backups</a></li>
              </ul>
            </li>
          <% } %>
          <% if (safeAdminUser) { %>
            <li class="nav-item"><a class="nav-link" href="/admin/accounts">Accounts</a></li>
          <% } %>

          <li class="nav-item"><a class="nav-link" href="/admin/logs">Logs</a></li>
        <% } %>
      </ul>
      <ul class="navbar-nav ms-auto navbar-minimal__secondary">
        <% if (safeAdminUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin/help">Help</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/account">My Account</a>
          </li>
        <% } %>
        <% if (safeAdminUser) { %>
        <li class="nav-item navbar-user-item">
          <span class="user-pill navbar-user-pill me-lg-3">
            <span class="user-name"><%= formatAdminRole(safeAdminUser.role) %></span>
            <span class="user-email"><%= safeAdminUser.email %></span>
          </span>
        </li>
        <li class="nav-item">
          <form method="post" action="/admin/logout" class="d-inline">
            <input type="hidden" name="_csrf" value="<%= safeCsrfToken %>" />
            <button
              class="btn btn-outline-light btn-sm"
              type="submit"
              data-confirm="Log out now?"
              data-confirm-title="Log out?"
              data-confirm-ok="Logout"
              data-confirm-cancel="Stay"
            >
              Logout
            </button>
          </form>
        </li>
        <% } else if (safeDiscordUser) { %>
        <li class="nav-item navbar-user-item">
          <span class="user-pill navbar-user-pill me-lg-3">
            <span class="user-name">Discord</span>
            <span class="user-email"><%= safeDiscordUser.username %></span>
          </span>
        </li>
        <li class="nav-item">
          <form method="post" action="/auth/logout" class="d-inline">
            <input type="hidden" name="_csrf" value="<%= safeCsrfToken %>" />
            <button
              class="btn btn-outline-light btn-sm"
              type="submit"
              data-confirm="Log out now?"
              data-confirm-title="Log out?"
              data-confirm-ok="Logout"
              data-confirm-cancel="Stay"
            >
              Logout
            </button>
          </form>
        </li>
        <% } else { %>
        <li class="nav-item me-2"><a class="btn btn-primary btn-sm" href="/auth/discord">User Login</a></li>
        <li class="nav-item"><a class="btn btn-outline-light btn-sm" href="/admin/login">Admin Login</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
